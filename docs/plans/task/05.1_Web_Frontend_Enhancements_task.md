# Task list for Phase 5.1: Web Frontend Enhancements & Dual-Camera Flow

## 1. Backend REST API Improvements (PUT/DELETE)
- [ ] Add PUT/DELETE endpoints for Devices & Cameras in `apps/parking-api/api/v1/devices.py`
- [ ] Add PUT/DELETE endpoints for Cards in `apps/parking-api/api/v1/cards.py`
- [ ] Add PUT/DELETE endpoints for Vehicles in `apps/parking-api/api/v1/vehicles.py`

## 2. Dual-Camera State Machine Implementation
- [ ] Modify `apps/parking-api/services/event_consumer.py`:
  - Fetch all cameras for the scan gate.
  - Setup multi-image tracking structure in Redis Cache.
  - Send snapshot requests to all gate cameras.
  - Listen to `camera.snapshot.completed`, update cache state, and proceed only when all camera snapshots are present.
  - Differentiate front camera (plate) and back camera (overview) roles when saving.

## 3. Frontend Authentication & RBAC
- [ ] Configure route guard constraints in `apps/parking-web/src/router/index.ts`
- [ ] Dynamic sidebar item visibility based on user role in `apps/parking-web/src/layouts/DefaultLayout.vue`

## 4. Frontend View Upgrades (Dual-Camera & CRUD)
- [ ] Update `LaneMonitor.vue` to show dual camera feeds (front/back) and 4 comparison slots on checkout
- [ ] Add edit/delete modal form in `Cards.vue`
- [ ] Add edit/delete modal form in `Vehicles.vue`
- [ ] Add edit/delete modal form in `Devices.vue` for cameras and hardware controllers

## 5. Verification
- [ ] Create integration test `test/05.1_web_frontend_enhancements_integration_test.py`
- [ ] Run pytest suite: `docker compose exec -T -e PYTHONPATH=. parking-api pytest test/05.1_web_frontend_enhancements_integration_test.py`
- [ ] Rebuild and test manually via browser on `http://localhost:3000`
