agent:
  id: "device-agent-gate-01"
  token: "CHANGE_ME_DEVICE_AGENT_TOKEN"
  type: "device"

gateway:
  url: "ws://parking-gateway:8300/ws/device-agent"
  heartbeat_interval: 30
  heartbeat_timeout: 90
  reconnect_interval: 5

storage:
  offline_queue_file: "/app/data/offline_events.json"
